//! Query bridge module — formerly adapted legacy `SearchParams` to the query engine.
//!
//! All callers now use `QueryCriteria` directly. This module is retained as
//! a placeholder for any future database-level query helpers.
